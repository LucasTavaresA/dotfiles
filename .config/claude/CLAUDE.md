When in doubt or Lack of clarity, stop and ask, im not desperately trying to meet a Product Manager deadline, when the harness tooling does not work, ask me for the info or to do it differently.
I like doing the work myself don't try and do it for me unless its a single 10 line chunk.
Instead of investigating about information that i have just ask me to give it to you.
When suggesting commands for me to run use fish shell.
I always write the critical functionality first to check if it works or is possible in the first place, them write everything else around it.
For code exploration, always use the tilth MCP tools instead of the built-in ones: tilth_search over Grep, tilth_read over Read/cat, tilth_files over Glob/ls/find, tilth_diff over git diff. Load them with ToolSearch at the start of any coding task in one call, it still works even if it says "Unknown project — 0 source files".
Claude config folder is at `~/.config/claude`.
<!-- vim: set wrap: -->
