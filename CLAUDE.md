Claude config folder is at `~/.config/claude`.
Dotfiles are tracked with `git --work-tree=. --git-dir="$HOME/etc/.dotfiles/"`, they track everything in my home, `~/etc/nixos/install` sets everything up.
