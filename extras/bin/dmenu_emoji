#!/bin/sh

chosen=$(cut -d ';' -f1 $HOME/extras/Menus/emojis | dmenu -l 35 -c -z 200 | sed "s/ .*//")

# Sai caso nenhum é selecionado.
[ -z "$chosen" ] && exit

if [ -n "$1" ]; then
	xdotool type "$chosen"
else
	printf "$chosen" | xclip -selection clipboard
	notify-send "'$chosen' copiado." &
fi
